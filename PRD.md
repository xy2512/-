# 技能互学 - 微信小程序 PRD（产品需求文档）

> 面向中国大学生群体的 C2C 技能交换与付费教学平台

## 1. 产品定位

- **目标用户**：18-28 岁大学生、年轻人，以及兼职教师、自由职业者、兴趣技能爱好者。
- **核心价值**：打造大学生群体的技能变现与低成本学习闭环，通过“先上课、后付费”模式降低跳单风险，建立初始信任。
- **商业模式**：首单平台抽佣 10%，后续课程免佣，支持脱离平台私下约课。

## 2. 核心功能模块

| 模块 | 关键功能 | 备注 |
|---|---|---|
| 用户系统 | 微信授权登录、绑定手机号、实名认证 | 实名认证为发布技能/收款的必要条件 |
| 技能发布 | 分类（运动/艺术/语言/IT/其他）、图文详情、价格、可约时间段 | 发布后需后台审核 |
| 发现与搜索 | 关键词搜索、多维度筛选（价格、教学方式、评分、地理位置） | 支持地图+列表双视图 |
| 预约与课表 | 老师可视化排课（周/月视图），学员一键预约空闲时段 | 系统自动冲突检测 |
| 订单与支付 | 第一节课：先上课→学员确认→支付→平台分账；后续课：可免佣 | 支持退款与仲裁 |
| IM 即时通讯 | 实时消息、会话列表、未读计数、消息已读 | WebSocket+轮询降级 |
| 评价与信用 | 双向互评、信用分、搜索权重 | 差评影响曝光 |
| 后台管理 | 内容审核、订单仲裁、违规封禁 | 运营后台（云开发控制台） |

## 3. 关键业务流程

### 3.1 首次预约 & 交易流程（差异化亮点）
1. 学员浏览老师详情 → 发起私聊咨询 → 确认意向  
2. 老师在「我的课表」标记可授课时段  
3. 学员选择时段 → 发起预约（状态：待上课，无需预付）  
4. 线上/线下完成教学交付  
5. 学员在小程序点击「确认下课」→ 发起支付（状态：已完成）  
6. 系统自动分账：平台抽佣 10%，剩余 90% 结算至老师微信零钱  
7. 标记该师生关系为“已成交”，后续预约免佣  

### 3.2 异常处理
- 学员爽约/未支付：老师可提交证据申请平台仲裁，平台核实后封禁或限制学员功能。
- 退款：12 小时内且已支付订单，学员可一键申请退款，原路退回。

## 4. 技术实现要点

- **前端**：微信小程序 + 云开发（数据库、云函数、云存储）
- **实时消息**：`db.collection.watch()` 监听，WebSocket 异常时自动降级为 3-5 秒轮询
- **支付**：微信支付云模板（`cloudbase_module`）统一处理下单、退款、分账
- **权限**：开发期集合权限设为“所有用户可读写”，上线前按最小权限收紧
- **环境**：所有云函数使用 `DYNAMIC_CURRENT_ENV`，客户端在 `app.js` 固定 `envId`，确保一致

## 5. 数据库核心集合

| 集合 | 核心字段 | 说明 |
|---|---|---|
| users | openId, phone, nickname, avatar, city, creditScore | 用户基础信息 |
| skills | teacherId, title, category, pricePerHour, schedule[] | 技能与可约时段 |
| bookings | userId, teacherId, skillId, status, payStatus, wxpay, refundStatus | 订单与支付 |
| conversations | participants[], lastMessage, lastMessageTime, unreadCount | 会话列表 |
| messages | conversationId, fromUserId, toUserId, content, type, isRead | 单条消息 |

## 6. 埋点与核心指标

- **转化漏斗**：技能详情 UV → 点击预约 UV → 预约成功 UV → 支付成功 UV
- **复购率**：同一对师生产生第二次交易的比例
- **履约率**：预约后完成支付的订单占比
- **搜索热词**：高频技能关键词，用于优化分类与推荐策略

## 7. 未来规划（Roadmap）

- v1.1：「附近的需求」板块，学员发布求助，老师抢单
- v1.2：高级会员体系（置顶、佣金减免、专属标识）
- v2.0：视跳单数据引入定金机制，平衡体验与平台风险

---

### 快速开始（给开发者的 README 精简版）

1. 用微信开发者工具导入 `front云` 目录  
2. 在云开发控制台**新建/选择环境**，记录 `envId`  
3. 在 `app.js` 中填写 `envId`，确保客户端与云函数环境一致  
4. 创建集合：`users`、`skills`、`bookings`、`conversations`、`messages`  
5. 部署云函数：`completeLesson`、`wxpayFunctions`（含下单/退款/转账）、`wxpayOrderCallback`、`wxpayRefundCallback`  
6. 安装微信支付云模板 `cloudbase_module`，配置商户号、API 密钥、证书、支付回调地址  
7. 开发期将集合权限设为“所有用户可读写”，上线前再按最小权限收紧  
8. 体验完整流程：注册 → 发布技能 → 预约 → 上课 → 支付 → 评价

如需查看详细字段、云函数入参、异常码对照，请继续阅读源码与注释。
